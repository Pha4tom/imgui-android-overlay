This directory is a git submodule pointing at [ocornut/imgui](https://github.com/ocornut/imgui).

```
git submodule update --init --recursive
```

CMake expects `imgui.h` here. The submodule is not vendored in this repository.
